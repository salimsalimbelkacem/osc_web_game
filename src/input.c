#include <raylib.h>
#include <emscripten/emscripten.h>

// what i want to do here is an iput api to call before/during the gameloop maybe

// TODO: implement maps for actions
// TODO: make a map with input -> action :P

void map_input(int key, void*func() /*, kb_input_map map?*/)
{
  
}

// TODO: make maps with screen area -> action :P
// since it'll be a point and click game, every scene will be a map kinda

void map_area(int key, void*func() /*, mouse_input_map map?*/)
{
  
}
